# UC2- Use Case 2
This branch contains the code that implements taking in user input and checking if the guessed number is correct with a loop that runs till the user guesses the correct number or the max attempts are over.
